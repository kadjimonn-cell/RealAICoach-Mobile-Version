apiVersion: apps/v1
kind: Deployment
metadata:
  name: autonomous-engine-service
  labels:
    app: autonomous-engine-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: autonomous-engine-service
  template:
    metadata:
      labels:
        app: autonomous-engine-service
    spec:
      containers:
        - name: autonomous-engine-service
          image: realaicoach/autonomous-engine-service:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "uvicorn", "modules.autonomous_service_app:app", "--host", "0.0.0.0", "--port", "8101"]
          ports:
            - containerPort: 8101
          env:
            - name: MONGO_URL
              valueFrom:
                secretKeyRef:
                  name: realaicoach-backend
                  key: MONGO_URL
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: realaicoach-backend
                  key: DB_NAME

---
apiVersion: v1
kind: Service
metadata:
  name: autonomous-engine-service
  labels:
    app: autonomous-engine-service
spec:
  selector:
    app: autonomous-engine-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8101
