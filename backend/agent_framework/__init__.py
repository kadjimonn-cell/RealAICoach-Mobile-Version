"""Native AI Agent Framework — provider-agnostic, modular, enterprise-ready.

Modules:
  providers  — provider abstraction layer (vendor-agnostic LLM access)
  agents     — role-based agent registry with versioning
  prompts    — reusable versioned prompt templates
  tools      — extensible tool/plugin registry
  memory     — standardized agent memory & context management
  policy     — centralized config, quotas, and policy enforcement
  engine     — workflow engine (sequential / parallel / conditional / human-in-the-loop)
  audit      — centralized audit logging
"""
