{
  "name": "realaicoach-web",
  "version": "1.0.0",
  "private": true,
  "description": "RealAICoach web frontend (React) — serves the shared cross-platform UI backed by the FastAPI backend.",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node scripts/build.js",
    "sync:rnw": "node scripts/sync-rnw.js",
    "test": "echo \"no web tests\" && exit 0"
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
